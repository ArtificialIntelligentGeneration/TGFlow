# MySLAVA

MySLAVA — это кроссплатформенное настольное приложение на Python (PyQt 6 + Pyrogram) для массовой рассылки сообщений в Telegram с нескольких аккаунтов.

## Возможности
* Добавление неограниченного числа аккаунтов по API ID / API Hash Telegram.
* Удобный редактор шаблонов сообщений (HTML → Markdown V2).
* Хранение шаблонов в локальной директории `scripts/`.
* Нацеливание рассылки на отдельные списки получателей для каждого аккаунта.
* Случайная задержка между сообщениями (анти-спам).
* Подробный HTML-лог каждой рассылки в `broadcast_logs/`.
* Все данные хранятся **локально** в директории пользователя (см. ниже).

## Быстрый старт
1. Установите Python 3.10 или новее.
2. Клонируйте репозиторий и создайте виртуальное окружение:

```bash
python3 -m venv venv
source venv/bin/activate   # Windows: venv\Scripts\activate
```

3. Установите зависимости:

```bash
pip install -r requirements.txt
```

4. Запустите приложение:

```bash
python main.py
```

## Сборка standalone-приложения
Используется PyInstaller. В репозитории есть готовые спек-файлы:

```bash
pyinstaller myslava.spec         # macOS
pyinstaller slava_aig_win.spec   # Windows
```

Собранный `.app` / `.exe` будет лежать в директории `dist/`.

## Где хранятся пользовательские данные
Все рабочие файлы помещаются в каталог пользователя:

* **macOS:** `~/Library/Application Support/SLAVA`
* **Windows:** `%APPDATA%\SLAVA`

Внутри него создаются:

* `accounts.json` — сохранённые аккаунты Telegram
* `broadcast_logs/` — HTML-отчёты о рассылках
* `scripts/` — шаблоны сообщений
* `sessions/` — файлы сессий Pyrogram

Эти директории добавлены в `.gitignore` и не попадают в репозиторий.

## Выбор чатов (ChatPicker)
На вкладке `Рассылка` для каждого аккаунта доступна кнопка `Чаты`. Нажмите её, чтобы открыть список ваших чатов (группы/супергруппы/каналы) для данного аккаунта.

- Поиск: фильтрация по названию или `@username`.
- Мультивыбор: выделите несколько чатов и нажмите `OK`.
- Результат: в поле «Лиды» автоматически подставятся адреса в формате `@username` или `-100…` по одному на строку.
- Обновление: кнопка `Обновить` перезагружает список без использования кэша.
- Индикаторы: при первой загрузке показывается индикатор выполнения; элементы с 🚫 — риск отсутствия прав на запись (каналы без админки, группы с ограничениями/баном), подробности — в тултипе.

Примечание: список чатов кэшируется в рамках работы приложения. Чтобы обновить список, перезапустите приложение или заново авторизуйте аккаунт.

Ограничения:
- В ChatPicker не включены личные сообщения (DM) и «Избранное».
- Для каналов без админских прав отметка 🚫 — предупреждение о риске невозможности публикации.
- Для приватных/ограниченных чатов возможны корректные отказы на этапе отправки; подробности — в логах.

## Dry‑Run и предпроверка получателей
- Чекбокс «Пробный запуск (без отправки)» — логирует шаги рассылки без реальной отправки.
- «Предпроверка получателей» — проверяет доступность чатов и права на запись (каналы/группы), отмечает риски до запуска.

## Медиа‑оптимизация
- Опция «Оптимизировать медиа»: уменьшение изображений до 1280px по большей стороне, JPEG ≈ 80, PNG optimize.
- Полезно для уменьшения веса и снижения вероятности ошибок по лимитам Telegram.

## Лицензия
Все права защищены © Artificial Intelligent Generation, 2025. Использование кода возможно только с письменного разрешения автора. 
