# Методика тестирования рассылки (Live Broadcast Testing)

Для проверки стабильности работы рассылки, корректности обработки блокировок сессий и соблюдения задержек используется автоматизированный скрипт.

## Скрипт: `tests/test_broadcast_live.py`

Этот скрипт эмулирует работу основного приложения, но запускается из командной строки, что позволяет быстро тестировать логику без использования GUI.

### Что делает скрипт:
1.  **Загружает окружение:** Подключает необходимые модули (`main.py`, `app_paths.py`, `script_manager.py`).
2.  **Загружает аккаунты:** Читает `accounts.json` из рабочей директории приложения.
3.  **Загружает сообщение:** Читает текст из файла «Тестовый скрипт.txt» в папке скриптов.
4.  **Настраивает получателей:** Отправляет сообщение заданному списку получателей (по умолчанию `@zakup2x2` три раза) для проверки волновой отправки.
5.  **Запускает Worker:** Инициализирует `OptimizedBroadcastWorker` с параметрами:
    *   Задержка между волнами: 10–20 секунд.
    *   Синхронный вывод логов в консоль.

### Как запустить

Откройте терминал в корне проекта и выполните:

```bash
./venv/bin/python tests/test_broadcast_live.py
```

### Интерпретация результатов

*   **Успех:** В консоли появляются логи отправки (`✅ #1 → @zakup2x2`), прогресс доходит до 100%, скрипт завершается без ошибок.
*   **Ошибка блокировки (Session locked):** Если в логе появляется `RuntimeError: Session is already used...` или `Deadlock`, это означает проблему с конкурентным доступом к файлу сессии.
    *   *Решение:* Проверьте, не запущены ли другие экземпляры приложения ("зомби-процессы").
*   **Ошибка авторизации:** Если лог показывает `Unauthorized`, значит, сессия аккаунта недействительна (нужен перелогин в основном приложении).

### Пример успешного вывода

```text
Initializing QCoreApplication...
Loading accounts from .../accounts.json...
Using account: HermannSaliter (+79604487797)
Loading script: Тестовый скрипт.txt...
Creating OptimizedBroadcastWorker...
Starting worker.run()...
[PROGRESS] 0%: Подготовка рассылки...
...
[WORKER LOG] HermannSaliter: ✅ #1 → @zakup2x2
[WORKER LOG] ⏳ Ожидание 12.5с до следующей волны...
...
[WORKER LOG] 🏁 Рассылка завершена!
```






