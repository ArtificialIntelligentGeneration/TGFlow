# План разработки и отладки TGFlow Mini

## 🔴 Текущая задача: Отладка загрузки чатов (Fix Chat Loading)

**Проблема:** При нажатии "Select Chats" происходило зависание или ошибка, список не загружался.
**Причина (Гипотеза H1):** Конфликт Event Loop между потоком GUI (PyQt) и библиотекой Pyrogram (asyncio). Клиент запускался не в том контексте.

### ✅ Выполненные действия
1.  **Рефакторинг `ChatListWorker`:**
    *   Переписана логика инициализации клиента.
    *   Клиент теперь стартует (`start()`) строго внутри нового Event Loop потока.
    *   Добавлен метод `open_client(..., just_instance=True)` для создания клиента без немедленного подключения.
2.  **Логирование:**
    *   Добавлена система записи отладочных логов в файл `mini_debug.jsonl` в корне проекта.
    *   Логи дублируются в консоль (stdout).
    *   Покрыты ключевые этапы: запуск воркера, открытие клиента, получение диалогов.
3.  **Обработка ошибок:**
    *   Добавлены проверки на наличие файла сессии.
    *   Добавлен `try-except` внутри цикла обработки диалогов, чтобы один сбойный чат не ломал весь список.

### 📋 План действий (Next Steps)

1.  **Проверка исправления (User Testing):**
    *   [ ] Запустить `run_mini.command`.
    *   [ ] Нажать "Select Chats".
    *   [ ] Убедиться, что список чатов загружается.
    *   [ ] Выбрать чаты и нажать OK.

2.  **Если ошибка сохраняется:**
    *   [ ] Прочитать файл `mini_debug.jsonl`.
    *   [ ] Найти строки с `"level": "ERROR"` или `"hypothesisId": "H1"`.
    *   [ ] Скорректировать код на основе логов (возможно, проблема в Proxy или версии Pyrogram).

3.  **Если успешно:**
    *   [ ] Удалить отладочное логирование (`_log_debug`).
    *   [ ] Протестировать отправку сообщений (Broadcast).
    *   [ ] Убедиться, что отправка по ID чата (а не username) работает корректно.

## 🚀 Будущие задачи (Backlog)

1.  **Выбор папок (Folders):**
    *   Реализовать логику `client.get_folders()` (если поддерживается Pyrogram) или фильтрацию диалогов.
    *   Добавить шаг выбора папки перед выбором чатов (как просил пользователь).
2.  **UI/UX:**
    *   Улучшить внешний вид окна выбора чатов.
    *   Добавить отображение аватарок (опционально, сложно в реализации на PyQt).
3.  **Оптимизация:**
    *   Кэширование списка чатов (частично реализовано).







